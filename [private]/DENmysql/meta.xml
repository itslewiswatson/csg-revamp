<meta>
	<info author="Jack" version="2.3" type="script" name="DENmysql" description="MySQL configuration resource" />
	
	<script src="mysql.lua" protected="true" />
	<!--<script src="mysql_backup.lua"/> --> <!-- remove commenter off if new version is bugged! -->
	
	<!-- Exported functions -->
	<export function="getConnection"/> <!-- returns the MySQL connection (if there is one) -->
	<export function="query"/> <!-- get a chunk of data from the database -->
	<export function="querySingle"/> <!-- get 1 row of data from the database -->
	<export function="exec"/> <!-- execute something directly into the database -->
	<export function="doesColumnExist"/> <!-- check see if a column exists in a table -->
	<export function="creatColumn"/> <!-- creates a column inside a table -->
</meta>
